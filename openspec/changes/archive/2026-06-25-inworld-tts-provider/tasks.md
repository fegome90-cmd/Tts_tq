# Tasks: Inworld TTS Provider + Speaker Threading

TDD order per task: RED (failing test first) → GREEN (minimal impl) → REFACTOR. Tests reference `_post` override seam, not urllib monkey-patch.

## Phase 1: Domain exception (foundation — depended on by adapter + clone guard)

- [x] 1.1 RED — `tests/unit/test_exceptions.py`: add `test_unsupported_operation_error_is_tts_error` + `test_unsupported_operation_error_requires_operation_provider_kwargs` (assert kw-only `operation`, `provider`; `.operation`/`.provider` attrs). Satisfies spec req `clone_voice typed refusal`.
- [x] 1.2 GREEN — `src/tts_lab/domain/exceptions.py`: add `UnsupportedOperationError(TTSError)` ctor `(self, message, *, operation: str, provider: str)`; update `__all__`.

## Phase 2: Config (foundation — depended on by factory + adapter)

- [x] 2.1 RED — extend `tests/unit/` (new `test_config.py` if absent else reuse): `test_tts_config_provider_defaults_qwen`, `test_tts_config_from_env_reads_tts_provider`, `test_inworld_config_from_env_reads_key_base_url_voice_encoding`.
- [x] 2.2 GREEN — `src/tts_lab/infrastructure/config.py`: add `provider: str = "qwen"` to `TTSConfig`; read `os.getenv("TTS_PROVIDER", "qwen")` in `from_env()`. Add frozen `InworldConfig` + `from_env()` reading `INWORLD_API_KEY` (raise nothing here — eager check is factory's job), `INWORLD_BASE_URL` (default `https://api.inworld.ai`), `INWORLD_AUDIO_ENCODING` (default `LINEAR16`), `INWORLD_DEFAULT_VOICE_ID` (default `Sarah` — see Phase 6 resolution).

## Phase 3: Speaker threading (application + edge — latent bug fix; safe before adapter)

- [x] 3.1 RED — `tests/unit/test_use_cases.py`: add `test_execute_threads_speaker_into_tts_request` (DTO speaker="Dennis" → `TTSRequest.speaker == "Dennis"`), `test_execute_default_speaker_is_none` (regression guard: no speaker → `TTSRequest.speaker is None`, NOT "Serena"). Add `test_generate_speech_request_has_speaker_default_none` in `TestDTOs`. Satisfies spec req `Speaker threading end-to-end` + Qwen regression scenario.
- [x] 3.2 GREEN — `src/tts_lab/application/dto.py`: add `speaker: str | None = None` to `GenerateSpeechRequest` (after `voice_profile_name`); update docstring + `__all__` unchanged.
- [x] 3.3 GREEN — `src/tts_lab/application/use_cases.py`: in `execute`, build `TTSRequest(text=..., language=..., speaker=request.speaker)`.

## Phase 4: Inworld adapter (core — depends on Phases 1 & 2)

- [x] 4.1 RED — new `tests/unit/test_inworld_client.py`: `TestStripIntermediateWavHeaders` — unit-test pure helper with hand-crafted 3-chunk WAV bytes → exactly 1 RIFF header, sf.read returns full duration. Satisfies spec req `Single-WAV output for multi-chunk LINEAR16`.
- [x] 4.2 GREEN — `src/tts_lab/infrastructure/inworld_client.py`: module-level pure `strip_intermediate_wav_headers(chunks: list[bytes]) -> tuple[bytes, int]` (parse chunk-1 RIFF for sample_rate+PCM; chunks 2..N strip RIFF keep PCM; rebuild one RIFF wrapper; return `(wav_bytes, sample_rate)`).
- [x] 4.3 RED — `test_inworld_client.py`: `TestPostSeam` — override `_post` via subclass/factory returning BytesIO of canned NDJSON; 7 edge cases — (a) happy single-chunk, (b) BOM first line stripped, (c) `\r\n` tolerated, (d) error object → TTSError sanitized, (e) non-JSON line → TTSError("malformed NDJSON line"), (f) line with `result` but empty/missing `audioContent` → SKIPPED (no bytes accumulated, not an error), (g) partial/truncated final line (no trailing newline / truncated JSON) → end-of-stream after last valid line; if NO valid line was seen → TTSError("empty/truncated response"). Satisfies spec reqs `Inworld NDJSON generate contract`, `NDJSON edge-case handling` (all 7 sub-clauses a-f).
- [x] 4.4 RED — `test_inworld_client.py`: `TestGenerateHappyPath` (AudioResult sample_rate == sampleRateHertz; voiceId = request.speaker overrides default); `TestTextLength` (`len(text) > 2000` → TTSError, `_post` never called). Satisfies reqs `Text length validated before network`, speaker-override scenario.
- [x] 4.5 RED — `test_inworld_client.py`: `TestExceptionWrapping` — `_post` raising each of HTTPError, URLError, socket.timeout, ssl.SSLError, OSError → TTSError subclass; malformed NDJSON line → JSONDecodeError wrapped; corrupt base64 audioContent → binascii.Error wrapped. Satisfies req `Exhaustive urllib/json/binascii exception wrapping`.
- [x] 4.5b RED — `test_inworld_client.py`: `TestUrllibWrapHelper` — unit-test the pure `_wrap_urllib_error(e)` helper (extracted in 4.8b) DIRECTLY (no urllib monkey-patch): construct each of `urllib.error.HTTPError`, `urllib.error.URLError`, `socket.timeout`, `ssl.SSLError`, `OSError` and assert `_wrap_urllib_error` maps them to the correct infra subclass (`InworldAPIError` / `InworldConnectionError`). PREFERRED over patching `urllib.request.urlopen` — a pure helper is deterministic, fast, and protects the 80% coverage gate (7.1) for the default `_post` urllib path without brittle stdlib patching. Satisfies req `Exhaustive urllib/json/binascii exception wrapping` (default `_post` branch).
- [x] 4.6 RED — `test_inworld_client.py`: `TestSanitization` — HTTP non-2xx `_post` returns body containing fake-key + `Authorization: Basic ...` substring → TTSError message contains NONE of {key, "Basic", request body}; body truncated ≤512 chars. Satisfies req `Key-leak sanitization`.
- [x] 4.7 RED — `test_inworld_client.py`: `TestCloneRefusal` (`clone_voice` raises `UnsupportedOperationError(operation="clone_voice", provider="inworld")`); `TestNoOpContextManager` (`__enter__` returns self, `__exit__` returns None, no protocol change needed); `TestDurationApproximate` (LINEAR16 ≈ len(pcm)/sr, MP3 == 0.0). Satisfies reqs `clone_voice typed refusal`, `No-op context manager`, `Duration is approximate`.
- [x] 4.8a GREEN — `src/tts_lab/infrastructure/inworld_client.py`: `InworldTTSClient(config: InworldConfig)` skeleton implementing `TTSClient`. Happy-path `generate`: pre-check `len(text) > 2000` → TTSError; build payload (voiceId = request.speaker or config.default_voice_id; audioConfig LINEAR16/MP3 + sampleRateHertz); call `self._post(payload)` seam → read NDJSON; happy single-chunk line parse (strip BOM first line, tolerate `\r\n`, `json.loads` per line, base64-decode `result.audioContent`); if LINEAR16 → `strip_intermediate_wav_headers`; duration LINEAR16 ≈ len(pcm)/sr else 0.0; return `AudioResult`. No-op `__enter__` returns self / `__exit__` returns None. `clone_voice` raises `UnsupportedOperationError(message, *, operation="clone_voice", provider="inworld")`. `_post` left as a stub seam (raise `NotImplementedError`) — 4.8b fills the default. Satisfies 4.1, 4.3(a), 4.4.
- [x] 4.8b GREEN — `inworld_client.py` default `_post(payload)`: stdlib `urllib.request` POST to `{base_url}/tts/v1/voice:stream` with `Authorization: Basic {api_key}` header; return raw response bytes. Extract a PURE module-level helper `_wrap_urllib_error(e: BaseException) -> TTSError` that maps `urllib.error.HTTPError` → `InworldAPIError`, `urllib.error.URLError`/`socket.timeout`/`ssl.SSLError`/`OSError` → `InworldConnectionError` (each subclass of domain `TTSError`). `_post` wraps its `urllib.request.urlopen` call in try/except delegating to `_wrap_urllib_error`. The pure helper is unit-tested directly by 4.5b (no urllib monkey-patch). Satisfies 4.5, 4.5b.
- [x] 4.8c GREEN — `inworld_client.py` sanitization + remaining NDJSON edge cases. Sanitization fn: regex-redact any `INWORLD_API_KEY`-shaped token + the literal `Authorization: Basic ...` value + the substring `Basic`; truncate body ≤512 chars; drop non-ASCII. `generate`'s NDJSON loop extended beyond happy-path to cover 4.3(b)-(g): BOM strip, `\r\n` tolerate, `error` object → sanitized `TTSError`, non-JSON line → `TTSError("malformed NDJSON line")`, empty/missing `audioContent` → skip (no error), partial/truncated final line → end-of-stream after last valid line / no valid line → `TTSError("empty/truncated response")`. `JSONDecodeError`/`binascii.Error` raised inside the loop wrapped → `InworldParseError`. HTTP non-2xx (surfaced via `InworldAPIError` from 4.8b) carries sanitized body. Satisfies 4.3(b-g), 4.6.
- [x] 4.8 NOTE (apply guidance) — implement incrementally; satisfy each driving RED test (4.1 → 4.3 → 4.4 → 4.5/4.5b → 4.6 → 4.7) before moving to the next sub-task (4.8a → 4.8b → 4.8c). The split is mechanical — granular REDs already exist; do NOT batch all three GREENs at once.

## Phase 4.9: Live-API integration test (DEFERRED)

- [x] 4.9 DEFERRED — `tests/integration/test_inworld_live.py` (`@pytest.mark.slow`, real Inworld API, multi-chunk LINEAR16 full-duration verification) is **DEFERRED**. The deterministic header-strip unit test 4.1 (`TestStripIntermediateWavHeaders` — hand-crafted 3-chunk WAV → exactly 1 RIFF header, `sf.read` returns full duration) is the ACCEPTED SUBSTITUTE and satisfies the "OR header-strip strategy" clause in proposal.md:157. Downgrade any "full-duration verified" wording to: "header-strip verified deterministically; live-API verification deferred (key rotation in progress)." No code action in this phase — only documentation of the deferral decision.

## Phase 5: Factory + CLI wiring (integration — depends on Phases 2, 3, 4)

- [x] 5.1 RED — new `tests/unit/test_tts_provider.py`: `test_create_qwen` (settings.provider="qwen" → QwenTTSClient); `test_create_inworld_with_key` (→ InworldTTSClient); `test_create_inworld_without_key` (`INWORLD_API_KEY` unset → TTSError-typed config error at construction); `test_create_unknown_raises_value_error`. Satisfies req `Factory provider selection` (3 scenarios).
- [x] 5.2 GREEN — `src/tts_lab/infrastructure/tts_provider.py`: `create_tts_client(settings: TTSConfig) -> TTSClient`; if `settings.provider == "qwen"` → `QwenTTSClient(model_path=settings.model_path, device=settings.device)`; if `"inworld"` → `InworldConfig.from_env()` eager; missing key → `raise TTSError("INWORLD_API_KEY ...") from KeyError`; else `raise ValueError(f"Unknown provider: {settings.provider}")`.
- [x] 5.3 GREEN — `src/tts_lab/cli.py` `generate` command: replace `with QwenTTSClient(...)` with `with create_tts_client(config) as client:`; pass `speaker=speaker` into `GenerateSpeechRequest(...)`. Pure drop-in — no signature change, no config refactor. Satisfies MODIFIED req `Speaker threading end-to-end` (CLI side).
- [x] 5.4 GREEN — `src/tts_lab/cli.py` `clone` command: at entry, after building `config = TTSConfig(...)`, add guard `if config.provider == "inworld": console.print typed refusal; raise typer.Exit(1)`. Prevents confusing Qwen model-load under inworld. Satisfies proposal risk `Clone asymmetry`.

## Phase 6: Open questions resolution + regression snapshot

- [x] 6.1 DECISION — `INWORLD_DEFAULT_VOICE_ID`: pin default `"Sarah"` in `InworldConfig.from_env()` (documented Inworld preset voice; overridable via env). Recorded in design open-question (a).
- [x] 6.2 DECISION — create `.env.example` with `INWORLD_API_KEY=` placeholder + `TTS_PROVIDER=qwen` + `INWORLD_DEFAULT_VOICE_ID=Sarah` + `INWORLD_AUDIO_ENCODING=LINEAR16` + `INWORLD_BASE_URL=https://api.inworld.ai`. Confirmed absent today; conventional for onboarding. Recorded in design open-question (b).
- [x] 6.3 RED — `tests/unit/test_use_cases.py`: `TestQwenDefaultSpeakerContractGuard` (NOT "byte-identical" — relabeled per gate-3) — contract guard asserting the default no-`-s` Qwen path: `GenerateSpeechRequest(text="x")` (speaker=None) → `TTSRequest.speaker is None` → Qwen maps to "Serena" via `qwen_client.py:96` (`request.speaker or "Serena"`). This is a CONTRACT lock (input wiring), not an output-byte snapshot — model load is infeasible at unit-test speed. Explicitly: the `-s <name>` path is a DELIBERATE BEHAVIOR CHANGE per the HONOR decision (spec MODIFIED req `Qwen -s semantics — HONOR`), NOT a regression — `request.speaker or "Serena"` means `-s Dennis` now intentionally changes Qwen's voice. Satisfies spec req `Qwen -s semantics — HONOR` default-path scenario.

## Phase 7: Verification (no new impl — green checks)

- [x] 7.1 `uv run pytest tests/unit/ -v --cov=src --cov-report=term-missing` — all green, coverage ≥80%.
- [x] 7.2 `uv run mypy src/` + `uv run ruff check src/` — clean.
- [x] 7.3 Traceability check: every spec requirement (13) + scenario (19) maps to ≥1 task above. Y.

## Notes
- `--instruct` remains dead code OUT OF SCOPE (entities.py:24 field + qwen_client.py:97 consume; cli.py:146-148 accepts; dto/use_case drop). Documented in spec NON-GOALS.
- Domain `protocols.py` + `entities.py` + `qwen_client.py` + `file_storage.py` UNCHANGED.
- No new direct dependency — stdlib `urllib.request`, `json`, `base64`, `ssl`, `socket`, `binascii` only.
